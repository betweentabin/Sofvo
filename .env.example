# Railway PostgreSQL Database Configuration
# Internal connection (for Railway deployment)
DATABASE_URL=postgresql://postgres:DNqaDqFjyphTNKTtazhhsJyRDFrPtNWz@postgres.railway.internal:5432/railway

# External connection (for local development)
# Get this from Railway dashboard -> PostgreSQL -> Connect -> Public Network
DATABASE_URL_EXTERNAL=postgresql://postgres:[PASSWORD]@[HOST]:[PORT]/railway

# Supabase Configuration (optional - if migrating from Supabase)
# Get these values from your Supabase project dashboard
# https://app.supabase.com/project/[YOUR-PROJECT-ID]/settings/api
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Application Settings
NODE_ENV=development
PORT=3000
VITE_API_URL=http://localhost:3001

# JWT Secret for authentication (generate a strong random string)
# You can generate one using: openssl rand -base64 32
JWT_SECRET=your-jwt-secret-here

# Push Notification Settings (optional)
PUSH_NOTIFICATION_SERVER_KEY=your-server-key
PUSH_NOTIFICATION_VAPID_KEY=your-vapid-key

# Note: Never commit your actual .env file to version control
# Copy this file to .env and fill in your actual values
# Server
PORT=5000
NODE_ENV=development

# Database (Railway PostgreSQL)
# ローカル開発では External URL を使用し、Railway 本番では DATABASE_URL を使用します。
# Railway Dashboard → PostgreSQL → Connect → Public Network で取得
DATABASE_URL_EXTERNAL=postgresql://postgres:password@host.railway.app:port/railway
# Railway 内部ネットワーク用（Railway 上での実行時）
# DATABASE_URL=postgresql://postgres:password@postgres.railway.internal:5432/railway

# JWT
JWT_SECRET=change-me-in-production
JWT_EXPIRE=7d

# Email Service (任意)
EMAIL_SERVICE=gmail
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password

# File Upload
UPLOAD_DIR=uploads
MAX_FILE_SIZE=5242880

# CORS
FRONTEND_URL=http://localhost:5173

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100
